name: nip_append_only_list
description: Local-first Dart implementation of the Nostr append-only lists NIP (kinds 1990/1991) on top of NDK.
version: 0.1.0
repository: https://github.com/nogringo/nip-append-only-list

environment:
  sdk: ^3.12.0

dependencies:
  ndk: ^0.8.4-dev.1
  broadcast_queue_shim_for_ndk: ^0.2.0
  sembast: ^3.8.7

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
